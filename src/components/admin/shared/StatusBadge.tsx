/**
 * Generalised status pill, keyed by the status string. Re-exports the shared
 * design-system adapter so the status → tone map lives in one place (ui/Badge).
 */
export { StatusBadge as default } from "@/components/ui/Badge";
